import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  container: {
    margin: 10,
  },
  input: {
    backgroundColor: '#eee',
    padding: 10,
    borderRadius: 10,
  },
});
